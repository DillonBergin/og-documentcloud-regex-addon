title: OG Regex Extractor
description: >-
  This will return a JSON file of all strings the given regex matches in
  the selected files
type: object
documents:
  - selected
  - query
properties:
  regex:
    title: Regex
    type: string
    format: regex
required: [regex]
categories: 
  - extraction 
